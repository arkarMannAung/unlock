import { createStore } from 'vuex';
import service from './modules/service';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    service
  }
})
